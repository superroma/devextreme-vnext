// Converted from: packages/dx-grid-core/src/plugins/integrated-selection/computeds.test.ts
// Phase: RED (Step 08)
import { describe, it } from 'vitest'

describe('useSelection - converted legacy behavior', () => {
  describe('rowsWithAvailableToSelect', () => {
    it.todo('should work (basic)')
    it.todo('should work with grouping (exclude group rows)')
  })
  describe('someSelected', () => {
    it.todo('should work with simple scenarios (edge combinations)')
    it.todo('should work when all available rows consist in selection rows')
    it.todo('should work when selection rows consist in available rows')
  })
  describe('allSelected', () => {
    it.todo('should work with simple scenarios (empty cases)')
    it.todo('should work when all available rows consist in selection rows')
  })
  describe('unwrapSelectedRows', () => {
    it.todo('should work (returns original rows)')
  })
})
