// Converted from: packages/dx-grid-core/src/utils/virtual-table.test.ts
// Phase: RED (Step 08)
import { describe, it } from 'vitest'

describe('utils/virtual-table - converted legacy behavior', () => {
  it.todo('should partition rows into virtual pages')
  it.todo('should compute virtual row heights')
})
